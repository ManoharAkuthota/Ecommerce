# ==============================================================================
# MS Mobiles Frontend — Production Environment Variables
# ==============================================================================

# In production behind a reverse proxy (e.g. Nginx), /api is routed to the backend container
VITE_API_BASE_URL=/api
